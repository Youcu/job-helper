# 오케스트레이터

```bash
python3 job_crawling_ochestrator.py
```

여섯 채용 사이트를 **병렬로** 돌리고, 끝나면 각 사이트 CSV 를 그대로 이어 붙여
`csv/merged.csv` 를 만든다.

```
Wanted · 사람인 · 잡코리아 · 잡플래닛 · 점핏 · Pathsdog
```

## 무엇을 하고, 무엇을 안 하나

| | |
|---|---|
| **한다** | 여섯을 동시에 돌린다 · 종료 코드를 읽어 성패를 가른다 · CSV 를 이어 붙인다 |
| **안 한다** | 중복 제거 · 정규화 · 거르기 · 어떤 전처리든 |

**합치기는 그대로 이어 붙이는 것뿐이다.** 같은 공고가 두 사이트에 있으면 두 행으로 남고,
표기가 어긋나도 손대지 않는다. 전처리는 다음 단계의 일이고, 여기서 손대면 원본이 무엇이었는지
되짚을 수 없게 된다.

**사이트별 CSV 는 그대로 둔다.** `csv/merged.csv` 는 사본이지 대체물이 아니다. 어느
사이트에서 온 행인지는 `사이트명` 칸에 남아 있고, 한 사이트만 다시 돌리고 싶으면
그 스크래퍼를 따로 부르면 된다.

## `.env` 는 **한 벌**이다

조건을 여섯 번 적지 않는다. 사이트마다 직무 코드 체계가 다르지만(Wanted `872`,
사람인 `84`, Pathsdog `Backend`) 사람이 원하는 것은 "백엔드 공고" 하나다.

```bash
JOB_ROLES=백엔드,웹                     # ← 직무를 여기 한 번만 적는다
YOE=0
HOME_LOCATIONS=서울,성남시
EMPLOYMENT_TYPES=regular,intern
EDUCATION=                              # ← 비워 둔다. 걸면 공고가 3분의 1로 준다
```

각 사이트가 `tags/<site>_role_map.json` 으로 자기 코드를 찾는다. **한 이름이 여러 코드로
펼쳐진다** — Wanted 에는 `자바 개발자`·`파이썬 개발자` 처럼 언어별 직무가 있어 `백엔드`
하나가 코드 넷이 된다 (`872` 서버 · `660` 자바 · `899` 파이썬 · `895` Node.js).

쓸 수 있는 이름은 `job_sites/_common/roles.json` 에 있다. 별칭도 받는다 —
`서버`·`Backend` 라고 적어도 `백엔드` 로 알아듣는다.

| 이름 | 이름 | 이름 | 이름 |
|---|---|---|---|
| 백엔드 | 프론트엔드 | 웹 | 풀스택 |
| 안드로이드 | iOS | 데브옵스 | 데이터엔지니어 |
| 데이터분석 | 머신러닝 | 보안 | QA |
| DBA | 게임 | 임베디드 | 블록체인 |

**사이트에 없는 직무는 조용히 빠지지 않는다.** `블록체인`은 잡플래닛에 대응 코드가 없는데,
그 사이트만 못 걸고 화면에 "이 사이트에 없는 직무라 못 걸었습니다" 라고 찍는다.

`JOB_ROLES` 가 비었거나 모르는 이름이면 그 사이트는 **종료 코드 1** 로 실패하고, 화면에
쓸 수 있는 이름을 보여 준다. 나머지 사이트는 그대로 돈다.

각 사이트가 조건을 어떻게 쓰는지는 `job_sites/<사이트>/README.md` 에 있다 — 같은 항목이
사이트마다 다르게 쓰인다(잡플래닛은 학력을 안 걸고, 점핏은 기술을 안 걸고, Pathsdog 는
근무지를 서버가 못 거른다).

## 종료 코드

각 스크래퍼가 내는 코드를 이렇게 읽는다.

| 코드 | 뜻 | 성패 |
|---|---|---|
| 0 | 정상 | **성공** |
| 1 | 설정 오류 | 실패 |
| 2 | 온전히 못 걷음 — 차단이거나 상세를 못 받음 (모은 것은 저장됨) | 실패 — 행이 있어도 온전하지 않다 |
| 3 | 이미 돌고 있음 | 실패 |
| **4** | **조건이 넓어 건너뜀** | **성공** |
| 그 밖 | 알 수 없음 | 실패 — 모르면 실패로 본다 |

**`4` 는 실패가 아니다.** 잡플래닛이 "이 조건은 절반만 걷힐 상황이라 아예 안 걷는다" 고
말하는 신호다. 실패로 세면 파이프라인 전체가 멎는다.

오케스트레이터 자신의 종료 코드는 **실패가 하나라도 있으면 1**, 없으면 0 이다.

## 왜 프로세스를 나누나

각 스크래퍼는 자기 `lib/` 를 `sys.path` 맨 앞에 넣는다. 한 프로세스에서 여섯을 부르면
먼저 불린 사이트의 `lib.config` 가 모듈 캐시에 남아 뒤엣것이 그것을 쓴다 — **조용히 남의
조건으로 긁는다.** 프로세스를 나누면 그럴 일이 없고, 하나가 죽어도 나머지가 산다.

겹쳐 도는 것은 각 사이트의 실행 자물쇠(`csv/.<사이트>.lock`)가 막는다. 오케스트레이터를
두 번 띄우면 나중 것의 사이트들이 **종료 코드 3** 을 낸다.

## 화면

자식의 출력은 붙잡아 둔다 — 여섯이 동시에 진행 막대를 그리면 화면이 엉킨다. 대신 막대
하나로 진행을 보이고, 끝난 사이트부터 한 줄씩 알린다.

```
사이트: 100%|██████████| 6/6
  ✓ wanted      12.3초 · 정상 · 141행
  ✓ jobplanet    8.1초 · 조건이 넓어 건너뜀 · 0행
  ✗ jumpit       0.4초 · 설정 오류
  …

사이트     종료     소요      행  뜻
wanted        0    12.3초    141  정상 · 141행
…
합계              512.7초    623  가장 오래 걸린 곳: saramin

csv/merged.csv — 623행
```

실패한 사이트는 마지막에 **스크래퍼가 한 말을 그대로** 보여 준다. "설정 오류" 라고만 하면
사람이 여섯 폴더를 뒤져야 한다.

## 걸리는 시간

가장 오래 걸리는 사이트에 맞춰진다 — 병렬이라 합이 아니다. 사람인이 8분대라 대략 그만큼이고,
한 사이트가 30분을 넘기면 끊는다.

## 테스트

```bash
python3 tests/run.py
```

34건 (일반 6 · 예외 8 · 경계 20). 자식 프로세스도 네트워크도 타지 않는다 — `_run_one` 을
가짜로 바꿔 넣는다. 테스트의 절반이 **종료 코드를 옳게 읽는가**를 굳힌다. 그리고
`SITES` 목록이 실제 폴더와 어긋나지 않는지도 본다 — 사이트를 하나 더 붙였는데 목록에
안 넣으면 조용히 빠진다.
