# 오케스트레이터

```bash
python3 job_crawling_ochestrator.py
```

다섯 채용 사이트를 **병렬로** 돌리고, 끝나면 각 사이트 CSV 를 그대로 이어 붙여
`csv/merged.csv` 를 만든다. 그 **뒤에** 수집 뒤 단계 다섯을 차례로 자식 프로세스로 부른다.

```
Wanted · 사람인 · 잡코리아 · 잡플래닛 · 점핏
        │
        ▼ 병렬 실행 후 이어 붙이기
   csv/merged.csv
        │
        ▼ 그림 본문만 골라 읽기 (job_image_process.py)
   csv/merged_read.csv
        │
        ▼ 중복 제거 · 낱말 제외 (filter.py)
   csv/merged_filtered.csv  +  csv/filter_report.csv
        │
        ▼ 잡플래닛 평점 (jobplanet_rating.py)
   csv/merged_rated.csv  +  csv/rating_report.csv  +  csv/same_names.csv
        │
        ▼ 핵심 기술 (core_stack.py)
   csv/merged_core.csv  +  csv/core_stack_report.csv
        │
        ▼ 경력 거르기 (career.py) — 신입이라 적고 경력을 요구하는 공고를 뺀다
   csv/merged_career.csv  +  csv/career_report.csv        ← 최종
        │
        ▼ 이력 쌓기 · 사이트 CSV 치우기 (history.py)
   history/history_read.csv · history_core.csv · history_dropped.csv · history_same_names.csv
```

**앞이 실패하면 뒤를 부르지 않는다.** 뒤 단계의 입력은 앞이 낸 파일인데, 앞이 죽었으면
거기 있는 것은 **지난 실행이 남긴 것**이다. 그걸 다듬으면 어제 결과가 오늘 것처럼 나온다 —
터지지 않고 조용히 틀리므로 알아채기가 가장 어렵다. 어느 단계가 실패하든 실행 전체의
종료 코드는 `1` 이다 — `0` 을 내면 자동화가 성공으로 읽는다.

### 단계 목록은 **표 하나**다

다섯 단계는 `job_crawling_ochestrator.py` 의 `STAGES` 표에 있고, 그 표를 돌면서
차례로 부른다. **단계를 늘리는 일은 표에 한 줄 더하기다.**

| 칸 | 쓰는 곳 |
|---|---|
| 스크립트 | 무엇을 자식으로 띄우나 |
| 이름 | 화면과 실패 메시지에 나오는 말 |
| 종료 코드 표 | 그 단계의 숫자를 뜻으로 옮긴다 ([아래](#단계마다-종료-코드-표가-다르다)) |
| 제한 시간 | 아래 표 |
| 앞 단계가 남긴 파일 | 실패했을 때 **무엇이 안 바뀌었나**를 말해 준다 |

전에는 단계마다 호출·출력·실패 메시지를 손으로 적고 **마지막에 `stages` 목록에도 또
적었다.** 단계 하나를 늘리면 이 파일 안 다섯 군데를 고쳐야 했고, 그중 목록을 빠뜨리면
**그 단계가 실패해도 종료 코드가 `0`** 이 됐다 — 화면에는 "실패했습니다" 가 찍히는데
자동화는 성공으로 읽는다. 터지지도 멈추지도 않는 실패다.

`tests/test_orchestrator.py` 의 `test_BOUNDARY_every_stage_in_the_table_fails_the_run`
이 **표를 돌면서** 확인하므로, 표에 한 줄을 더하면 검사도 같이 늘어난다.

**제한 시간은 단계마다 다르다.** 30분(`TIMEOUT_SECONDS`)은 스크래퍼 하나에 맞춘 값이다.

```
그림 판독      30분   스크래퍼와 같다
거르기          5분   실측 710행에 0.02초. 그물도 모델도 안 탄다
평점 거르기    180분   실측 457곳에 126분. 그물을 타고 5초씩 쉰다
핵심 기술       5분   파일 하나를 읽고 정규식을 돌릴 뿐이다
경력 거르기     20분   후보만 모델에 묻는다. 실측 66행 중 10건
이력 쌓기       5분   파일 몇 개를 병합해 쓴다
```

## 무엇을 하고, 무엇을 안 하나

| | |
|---|---|
| **한다** | 다섯을 동시에 돌린다 · 종료 코드를 읽어 성패를 가른다 · CSV 를 이어 붙인다 · 합친 뒤 네 단계를 차례로 자식으로 부른다 |
| **안 한다** | 합치면서 손대는 일 — 중복 제거도, 정규화도, 거르기도 **여기서는** 안 한다 |

**합치기는 그대로 이어 붙이는 것뿐이다.** 같은 공고가 두 사이트에 있으면 두 행으로 남고,
표기가 어긋나도 손대지 않는다. 버리는 것은 **`filter.py` 의 일**이고
([FILTER.md](FILTER.md)), 합칠 때 손대면 원본이 무엇이었는지 되짚을 수 없게 된다.

**사이트별 CSV 는 그대로 둔다.** `csv/merged.csv` 는 사본이지 대체물이 아니다. 어느
사이트에서 온 행인지는 `사이트명` 칸에 남아 있고, 한 사이트만 다시 돌리고 싶으면
그 스크래퍼를 따로 부르면 된다.

**수집 뒤 단계는 앞 단계의 파일을 읽기만 하고 고치지 않는다.** 넷 다 행을 없애므로,
제자리에서 고치면 없어진 행의 원본이 사라진다.

**이미지 판독은 `csv/merged.csv` 를 읽기만 하고 고치지 않는다.** 그림 본문을 읽어 채운
결과는 별도 파일 `csv/merged_read.csv` 로 나간다 — 이 단계는 행을 **없애기도** 하므로,
제자리에서 고치면 없어진 행의 원본이 사라진다. 자세한 것은
[image_process/README.md](image_process/README.md).

## `.env` 는 **한 벌**이다

조건을 사이트마다 적지 않는다. 사이트마다 직무 코드 체계가 다르지만(Wanted `872`,
사람인 `84`, 점핏 `1`) 사람이 원하는 것은 "백엔드 공고" 하나다.

```bash
JOB_ROLES=백엔드,웹                     # ← 직무를 여기 한 번만 적는다
YOE=0
HOME_LOCATIONS=서울,성남시
EMPLOYMENT_TYPES=regular,intern
EDUCATION=                              # ← 비워 둔다. 걸면 공고가 3분의 1로 준다
```

각 사이트가 `tags/<site>_role_map.json` 으로 자기 코드를 찾는다. **한 이름이 여러 코드로
펼쳐진다** — Wanted 에는 `자바 개발자`·`파이썬 개발자` 처럼 언어별 직무가 있어 `백엔드`
하나가 코드 넷이 된다 (`872` 서버 · `660` 자바 · `899` 파이썬 · `895` Node.js).

쓸 수 있는 이름은 `job_sites/_common/roles.json` 에 있다. 별칭도 받는다 —
`서버`·`Backend` 라고 적어도 `백엔드` 로 알아듣는다.

| 이름 | 이름 | 이름 | 이름 |
|---|---|---|---|
| 백엔드 | 프론트엔드 | 웹 | 풀스택 |
| 안드로이드 | iOS | 데브옵스 | 데이터엔지니어 |
| 데이터분석 | 머신러닝 | 보안 | QA |
| DBA | 게임 | 임베디드 | 블록체인 |

**사이트에 없는 직무는 조용히 빠지지 않는다.** `블록체인`은 잡플래닛에 대응 코드가 없는데,
그 사이트만 못 걸고 화면에 "이 사이트에 없는 직무라 못 걸었습니다" 라고 찍는다.

`JOB_ROLES` 가 비었거나 모르는 이름이면 그 사이트는 **종료 코드 1** 로 실패하고, 화면에
쓸 수 있는 이름을 보여 준다. 나머지 사이트는 그대로 돈다.

각 사이트가 조건을 어떻게 쓰는지는 `job_sites/<사이트>/README.md` 에 있다 — 같은 항목이
사이트마다 다르게 쓰인다(잡플래닛은 학력을 안 걸고, 점핏은 기술을 안 건다).

## 종료 코드

각 스크래퍼가 내는 코드를 이렇게 읽는다.

| 코드 | 뜻 | 성패 |
|---|---|---|
| 0 | 정상 | **성공** |
| 1 | 설정 오류 | 실패 |
| 2 | 온전히 못 걷음 — 차단이거나 상세를 못 받음 (모은 것은 저장됨) | 실패 — 행이 있어도 온전하지 않다 |
| 3 | 이미 돌고 있음 | 실패 |
| **4** | **조건이 넓어 건너뜀** | **성공** |
| 그 밖 | 알 수 없음 | 실패 — 모르면 실패로 본다 |

**`4` 는 실패가 아니다.** 잡플래닛이 "이 조건은 절반만 걷힐 상황이라 아예 안 걷는다" 고
말하는 신호다. 실패로 세면 파이프라인 전체가 멎는다.

오케스트레이터 자신의 종료 코드는 **실패가 하나라도 있으면 1**, 없으면 0 이다. 이미지
판독 단계의 실패도 여기 든다.

### 이미지 판독 단계는 **표를 따로 쓴다**

숫자 계약(0 정상 · 1 설정 · 2 온전치 못함 · 3 중복 실행)은 같지만 **그 코드가 가리키는
사건이 다르다.** 스크래퍼 표를 그대로 쓰면 사이트를 긁지도 않는 단계의 `2` 가 "차단이거나
상세를 못 받음" 으로 찍힌다 — 사람이 사이트 차단을 의심하며 엉뚱한 곳을 뒤진다.

| 코드 | 뜻 | 성패 |
|---|---|---|
| 0 | 정상 | **성공** |
| 1 | 단계를 못 돌림 — `merged.csv` 가 없거나 `claude` 명령을 못 찾음 | 실패 |
| 2 | 시도한 그림을 하나도 못 읽음 (`merged.csv` 는 그대로) | 실패 |
| 3 | 이미 돌고 있음 | 실패 |

`IMAGE_EXIT_MEANING` 이다. `4` 는 없다 — 이 단계에는 건너뛸 조건이 없다.

## 왜 프로세스를 나누나

각 스크래퍼는 자기 `lib/` 를 `sys.path` 맨 앞에 넣는다. 한 프로세스에서 여럿을 부르면
먼저 불린 사이트의 `lib.config` 가 모듈 캐시에 남아 뒤엣것이 그것을 쓴다 — **조용히 남의
조건으로 긁는다.** 프로세스를 나누면 그럴 일이 없고, 하나가 죽어도 나머지가 산다.

겹쳐 도는 것은 각 사이트의 실행 자물쇠(`csv/.<사이트>.lock`)가 막는다. 오케스트레이터를
두 번 띄우면 나중 것의 사이트들이 **종료 코드 3** 을 낸다.

## 화면

자식의 출력은 붙잡아 둔다 — 여럿이 동시에 진행 막대를 그리면 화면이 엉킨다. 대신 막대
하나로 진행을 보이고, 끝난 사이트부터 한 줄씩 알린다.

```
사이트: 100%|██████████| 5/5
  ✓ wanted      12.3초 · 정상 · 141행
  ✓ jobplanet    8.1초 · 조건이 넓어 건너뜀 · 0행
  ✗ jumpit       0.4초 · 설정 오류
  …

사이트     종료     소요      행  뜻
wanted        0    12.3초    141  정상 · 141행
…
합계              512.7초    623  가장 오래 걸린 곳: saramin

csv/merged.csv — 623행

이미지 본문 102건을 읽습니다 (동시 16개 · sonnet)
  캐시에서 바로       : 0건
  껍데기라 안 부름     : 3건 → 버림
  읽어서 채움         : 67건
  읽었는데 쓸 게 없음  : 30건 → 버림
  못 읽음 (그대로 둠)  : 2건
csv/merged_read.csv — 703행 (csv/merged.csv 736행에서 33건 버림)
```

이미지 판독 단계의 마지막 몇 줄을 그대로 이어 붙인다. 그 단계가 실패해도
`csv/merged.csv` 는 이미 나와 있으므로 걷은 것은 잃지 않는다 — 다만 오케스트레이터
자신의 종료 코드는 1이 된다.

실패한 사이트는 마지막에 **스크래퍼가 한 말을 그대로** 보여 준다. "설정 오류" 라고만 하면
사람이 폴더를 하나씩 뒤져야 한다.

**stderr 를 먼저 통째로(25줄까지) 보여 주고, 그다음 stdout 끝 8줄을 보여 준다.**
스크래퍼가 *왜 멈췄나* 를 말하는 자리는 stderr 다. stdout 은 진행 기록이라 양이 많아서,
끝 몇 줄만 보면 정작 이유가 뒤에 나온 안내문에 밀려 사라진다 — 실제로 잡플래닛의
`403 으로 막았습니다` 를 그렇게 잃었고, 403(차단)인지 429(속도 제한)인지 몰라 원인을
못 좁혔다. 403 과 429 는 대응이 정반대다.

## 걸리는 시간

가장 오래 걸리는 사이트에 맞춰진다 — 병렬이라 합이 아니다. 사람인이 8분대라 대략 그만큼이고,
한 사이트가 30분을 넘기면 끊는다.

## 테스트

```bash
.venv/bin/python3 tests/run_all.py   # 저장소 전체
.venv/bin/python3 tests/run.py       # 루트 묶음만
```

**건수는 여기 안 적는다** — 돌리면 찍힌다. 손으로 적어 두면 반드시 갈라진다.
자식 프로세스도 네트워크도 타지 않는다 — `_run_one` 과 `_run_stage` 를 가짜로 바꿔
넣고, `main()` 을 통째로 돌려 본다. 테스트의 절반이 **종료 코드를 옳게 읽는가**를 굳힌다.
그리고 `SITES` 목록이 실제 폴더와 어긋나지 않는지도 본다 — 사이트를 하나 더 붙였는데
목록에 안 넣으면 조용히 빠진다.

이미지 단계의 배선은 **글자가 아니라 동작으로** 본다. 예전에는 `inspect.getsource` 로
"합치기라는 글자가 이미지라는 글자보다 앞에 있는가" 를 봤는데, 그 호출이 통째로 사라져도
통과하거나 `ValueError` 로 터진다 — 실패가 아니라 오류로. 지금은 `main()` 을 돌려
**합친 뒤에 불렀는지**, **그 단계가 실패해도 합본이 남는지**, **어느 쪽이 실패하든 1 로
끝나는지**를 확인한다.
