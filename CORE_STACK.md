# 핵심 기술 거르기

```bash
python3 core_stack.py
```

`.env` 의 `CORE_TECH_STACKS` 가 적힌 공고만 남긴다. 뒤에 경력 거르기가 하나 더 있다
([CAREER.md](CAREER.md)).

```
csv/merged_rated.csv  ──▶  csv/merged_core.csv        남은 공고
      230행                     69행
                        +  csv/core_stack_report.csv  161행 — 뺀 이유 전량
```

숫자는 2026-09-11 실측(`CORE_TECH_STACKS=Spring, FastAPI`)이다. 돌리면 화면에 찍힌다.

## 무엇을 하고, 무엇을 안 하나

| | |
|---|---|
| **한다** | 세 칸을 보고 하나라도 걸리면 남긴다 · **뺀 것을 전량 보고 CSV 에 남긴다** |
| **안 한다** | 기술의 경중을 따지지 않는다 · 몇 개 걸렸는지로 순위를 매기지 않는다 |

하나라도 있으면 남긴다. **거르는 단계이지 점수 매기는 단계가 아니다.**

---

## 1. 세 칸을 다 본다 — `기술스택` 칸만 보면 안 된다

```
기술스택 · 지원자격 · 우대사항  —  어느 한 곳에라도 있으면 남긴다
```

**채용 사이트의 검색 조건에는 안 잡히는데 지원자격이나 우대사항에는 적혀 있는 공고가
있다** (2026-09-11 사용자 지적). 그래서 셋을 다 본다.

`공고명`·`근무지`·`기업명` 에는 안 건다 — 회사 이름과 지역명이 기술 이름을 품는다.

## 2. 왜 마지막 단계인가

이건 **걷는 조건이 아니라 고르는 조건**이다.

수집기에 걸면 위의 "검색에 안 잡히는 공고" 를 **아예 안 걷게 되고**, 나중에 기준을 바꿔도
다시 걷기 전에는 알 수 없다. 걷어 두고 마지막에 거르면 `.env` 한 줄을 고치고
`python3 core_stack.py` 만 다시 돌리면 된다 — 실측 1초다.

같은 이유로 `.env` 에 이미 있던 **`TECH_STACKS` 와는 다른 항목**이다. 앞은 수집기가 걷을
때 쓰라고 둔 자리이고(아직 아무 사이트도 안 건다), 이것은 다 걷어 온 뒤의 기준이다.

## 3. `Spring` 과 `Spring Boot` 는 서로를 잡는다

`Spring Boot` 로 검색하면 잘 안 나와서 `.env` 에 `Spring` 이라고 적어 두는데, 공고는 둘 중
아무 쪽으로나 쓴다. 실측 230행:

```
Spring 57번 · Spring Boot 43번 · Spring Framework 11번
Spring Cloud 2번 · Spring Security 2번 · SpringBoot 1번
```

하나로만 찾으면 **절반을 놓친다.** 그래서 양방향으로 잡는다 (사용자 지시).

| `.env` 에 적은 것 | 잡는 것 |
|---|---|
| `Spring` | `Spring` · `Spring Boot` · `Spring Framework` · `Spring Cloud` … |
| `Spring Boot` | 위 전부 **그리고 맨 `Spring`** |

앞쪽은 `Spring` 을 낱말로 찾으면 그대로 된다 — `Spring Boot` 안에 `Spring` 이 낱말로 있다.
뒤쪽은 **적어 준 이름의 첫 낱말로도 함께 찾아서** 된다.

**첫 낱말이 한 글자면 안 쓴다.** `C 언어` 를 적었다고 `C` 로 문서를 다 긁으면 그건 거르는
것이 아니라 긁어 오는 것이다.

## 4. 낱말로 찾는다 — 부분문자열이 아니다

```
찾는다      Spring · Spring Boot · Spring/JPA · (Spring) · Spring기반 · spring boot
안 찾는다   offspring · Springfield
안 찾는다   `Java` 를 적었을 때의 `JavaScript`   ← 부분문자열의 함정
```

앞뒤에 **ASCII 영숫자가 붙지 않았을 때만** 낱말로 본다. 한글은 경계로 안 친다.
대소문자는 안 가린다.

**`SpringBoot`(붙여 쓴 것)는 못 잡는다.** 실측 230행에서 1번 나왔다. 잡으려면 "뒤에 대문자가
오면 낱말이 끝난 것" 으로 봐야 하는데, 그 규칙은 `Java` → `JavaScript` 도 통과시킨다.

**틀린 이유로 남는 쪽이 더 나쁘다** — 빠진 것은 보고 CSV 에 남아 되짚을 수 있지만, 잘못
남은 것은 아무 표시가 없다. 이 맞바꿈은
`tests/test_core_stack.py` 의 `test_BOUNDARY_camel_case_run_together_is_not_found` 에 적혀 있다.

## 5. 비면 멈춘다

`CORE_TECH_STACKS` 가 비었을 때 "조건 없음" 으로 읽어 전부 통과시키면, **사람은 걸렀다고
믿는데 실제로는 아무것도 안 걸린 파일을 받는다.** 종료 코드 `1` 로 멈춘다.

## 6. 종료 코드

| 코드 | 뜻 |
|---|---|
| 0 | 정상 |
| 1 | `csv/merged_rated.csv` 가 없거나 `.env` 의 `CORE_TECH_STACKS` 가 비었다 |
| 3 | 이미 돌고 있다 |

`2`(부분 실패)와 `4`(건너뜀)는 이 단계에 없다 — 없는 사건을 표에 적으면 있지도 않은 일이
화면에 찍힌다.

`1` 의 뜻에 **`.env` 항목 이름이 들어간다.** 이 단계만 그 값을 읽으므로, 다른 단계의 표를
빌려 쓰면 "단계를 못 돌림" 이라고만 찍혀 어디를 고쳐야 할지 못 짚는다.

## 7. 테스트

```bash
python3 tests/run_all.py     # 저장소 전체
```

`tests/test_core_stack.py`. 그물도 `.env` 도 안 탄다 — 경로를 인자로 넣는다.

두 방향을 다 굳힌다. **너무 넓으면** `Java` 를 적었는데 `JavaScript` 공고가 남고(틀린 이유로
남음), **너무 좁으면** `Spring` 을 적었는데 `Spring Boot` 공고가 사라진다(멀쩡한 자리가 사라짐).
