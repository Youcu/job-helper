# 평점 거르기

```bash
python3 jobplanet_rating.py
```

잡플래닛에서 회사 평점을 걷어 **선 아래를 뺀다.**

```
csv/merged_filtered.csv  ──▶  csv/merged_rated.csv     남은 공고
      559행                        219행
                           +  csv/rating_report.csv    340행 — 뺀 이유 전량
                           +  csv/same_names.csv       146행 — 동명 회사 49곳의 후보
```

숫자는 2026-09-11 실측이다. 실행할 때마다 화면에 찍히므로 지금 값은 돌려 보면 안다.

## 선 (사용자가 정했다, 2026-09-11)

| 상태 | 어떻게 |
|---|---|
| 평점 **2.9 이상** | 남긴다 |
| 평점 2.9 미만 | 뺀다 |
| 검색은 됐는데 **평점이 없다** (`0.0`) | 뺀다 |
| **검색이 안 된다** | 뺀다 |

실측 457곳: 찾음 398 · 못 찾음 59. 뺀 340행의 내역은 `2.9미만 193 · 평점없음 77 · 검색안됨 70`.

---

## 1. 사람용 검색 페이지는 **못 쓴다**

처음에는 `/search/companies` 를 쓰려 했다. 못 쓴다. 각 40곳씩 실측했다.

| | HTML `/search/companies` | **API `/api/v3/search/companies`** |
|---|---|---|
| 완주 | **30곳 중 27번째에서 포기** | 40곳 전부 |
| 403 | 51% | 4% |
| 백오프 | 30→60→120→240→480→960초로 **악화되다 끝내 안 풀림** | **30초 한 번이면 늘 풀림** |
| 응답 | 111KB (HTML 통째) | **2KB (JSON)** |
| 곳당 | 31.6초 | **6.6초** |

API 주소는 검색 페이지의 JS 번들에서 찾았다 — `_next/static/chunks/app/(commonLayout)/search/companies/page-*.js`.
**정적 파일이라 Cloudflare 가 세지 않는다.** 차단당한 상태에서도 받아진다.

값도 구조화돼 온다. 정규식으로 HTML 을 긁지 않으니 사이트가 마크업을 바꿔도 안 깨지고,
`업종·사원수·업력·공고수` 가 함께 와서 **동명 회사를 가릴 재료**가 생긴다.

```json
{"company_id": 67099, "name": "(주)안랩", "rate_total_avg": 3.5,
 "industry_name": "IT/웹/통신", "company_headcounts": 1187,
 "company_years": "31년차 (1995)", "posting_counts": 1, "city_name": "경기"}
```

---

## 2. 안전 하한은 5초. **403 을 0 으로 만들 수는 없다**

간격을 넓히면 403 이 사라질 줄 알았는데 아니었다.

| 간격 | 403 | 곳당 | 457곳 환산 |
|---|---|---|---|
| 2.5초 | 14% | 7.90초 | 60분 |
| **5.0초** | **4%** | **6.61초** | **50분** |
| 8.0초 | 6% | 10.4초 | 79분 |

**8초가 5초보다 403 이 많다.** 간격만 보는 리미터가 아니라는 뜻이다 — 같은 문 앞에 우리 말고
다른 트래픽이 있다. 그러니 "403 이 안 나는 간격" 을 찾는 것은 헛일이다.

그래서 재현성의 근거를 옮겼다. **403 이 나느냐가 아니라 늘 같은 값으로 끝나느냐**다.

```
한 질의가 BACKOFF_LIMIT(3)번 넘게 연속 403 이면 멈춘다.
```

프로브 120곳에서 403 은 **예외 없이 30초 한 번에** 풀렸다. 그보다 오래 막히는 것은 실측과
다른 일이고, 거기서 더 두드리면 HTML 경로에서 본 악화가 시작된다.

**멈춰도 잃는 것이 없다.** 회사 하나를 끝낼 때마다 캐시에 원자적으로 쓰므로 다음 실행이
남은 곳부터 이어받는다 (종료 코드 2).

### 작은 표본을 믿지 않기를 잘했다

프로브 120곳에서는 연속 실패가 **한 번도** 없었다. 그런데 전량 457곳(요청 739건)에서는
**9건이 두 번 연속** 403 이었다. 3번 연속은 없었다. 상한을 2 로 뒀다면 그 9곳에서 멈췄다.

전량 실행 실측: **126분 · 요청 739건 · 403 78건(10%) · 중간 정지 없음.**

---

## 3. 이름 변형 사다리

회사명을 그대로 넣으면 안 나오는 경우가 있다. 앞에서 맞으면 **뒤는 안 묻는다** —
요청을 아끼는 것이 곧 차단을 피하는 것이다.

| 순서 | 변형 | 예 | 실측으로 살린 곳 |
|---|---|---|---|
| 1 | 원문 | `(주)안랩` | 249 |
| 2 | 법인표기 제거 | `㈜아무개` → `아무개` | 8 |
| 3 | 공백 제거 | `제로데이 시큐리티` → `제로데이시큐리티` | 3 |
| 4 | 괄호 제거 | `더즌(dozn)` → `더즌` | 9 |
| 5 | 괄호+공백 제거 | | |
| 6 | 괄호 안쪽 | `도미노피자(청오디피케이)` → `청오디피케이` | |
| 7 | 첫 어절 | `투모로 로보틱스` → `투모로` | 1 |
| 8 | 법인표기 추가 | `아무개` → `(주)아무개` | |

**두 글자 미만 질의는 안 던진다.** `가` 로 검색하면 이름에 `가` 가 든 회사가 쏟아지고,
그중 하나에 평점이 붙으면 엉뚱한 근거로 공고가 살거나 죽는다.

### 이름이 **실제로 같은 것만** 고른다

잡플래닛 검색은 부분 일치까지 준다. `안랩` 을 물으면 이렇게 온다.

```
(주)안랩 3.5 · (주)비안랩 1.7 · (주)시안랩 1.4 · (주)두리안랩 4.0 · (주)안랩클라우드메이트 0.0
```

첫 결과를 그냥 쓰면 안 되고, 느슨하게 맞추면 **`두리안랩`(4.0)의 평점으로 `안랩` 공고가
살아남는다.** 그래서 두 단계로 맞춘다.

1. **엄격** — 법인 표기와 공백만 지우고 비교한다 (`tight`)
2. 엄격한 맞춤이 **하나도 없을 때만** 괄호 부가설명까지 지우고 다시 본다 (`loose`)

엄격한 쪽이 하나라도 맞으면 느슨한 쪽은 안 본다.

---

## 4. 같은 이름이 여럿이면 — `same_names.csv`

`제일산업` 은 이름이 똑같은 곳이 넷이고 평점이 1.6·1.8·2.0·2.6 이다. 공고에는 사업자번호도
주소도 없어서 **어느 쪽인지 기계적으로 가릴 재료가 없다.**

지금은 **가장 높은 평점**을 그 회사로 본다. 이 단계는 행을 지우는 일이라 애매하면 남기는
쪽으로 기운다 — 잘못 지우면 그 공고가 있었다는 사실조차 안 남는다.

**그리고 후보를 전량 남긴다.** 다음 단계가 공고명·지원자격·우대사항·기술스택으로 한 회사를
특정할 수 있도록 `업종·사원수·업력·공고수·지역` 을 함께 적는다. 실측 49곳 · 146행이고,
후보가 가장 많은 곳은 `국제전기(주)` 10개 · `제일산업(주)` 9개다.

---

## 5. 웹검색으로 이름을 찾아 주는 길은 **뒀다가 들어냈다**

변형을 다 써도 0건인 곳을 모델에게 넘겨 정식 상호를 찾게 했었다. 실측 59곳을 넘겨
8곳을 "되찾았는데" **그중 절반이 엉뚱한 회사**였다.

```
바카티오(Vacatio) → 바티오(주)       평점 5.0 으로 4행이 통과했다. 이름부터 다르다
에이아이씨엑스     → 마이리얼트립(주)   평점 3.6. 아무 관계가 없다
```

구조가 잘못돼 있었다. 코드는 **모델이 준 이름과 검색 결과가 같은지**만 봤고, 그 이름이
원래 회사와 같은 회사인지는 아무도 안 봤다. 모델이 자신 있게 엉뚱한 이름을 주면 그대로
통과한다. 실측 **10행**이 그렇게 살아남았다.

**이 방향의 오류가 더 나쁘다.** 잘못 지운 것은 `rating_report.csv` 에 남아 되짚을 수 있지만,
**잘못 통과한 것은 아무 표시 없이 결과에 섞인다.**

사용자의 판단으로 들어냈다 — *"잘못된 데이터가 들어오느니 막는 게 맞고, 어차피 그렇게 해도
검색이 안 되는 곳이면 애초에 갈 곳이 못 된다."*

같은 이유로 **끝내 못 찾은 59곳도 그대로 뺀다.** 신생 스타트업이라 아직 리뷰가 없을 뿐일
수도 있지만 — *"최소 1년 이상 근무할 곳인데 가능성에 도박하고 싶지 않다."*

되살리려는 사람이 있으면 `tests/test_jobplanet_rating.py` 의
`test_BOUNDARY_there_is_no_web_search_fallback` 이 먼저 막는다.

---

## 6. 남는 것

| 파일 | 무엇 |
|---|---|
| `cache/company_rating.json` | 회사 457곳 기록. **이어받기의 근거** |
| `cache/company_rating.log.jsonl` | 요청 전량 — 질의·변형단계·상태·ms·바이트·결과수·경과 |

요청 로그는 **다음에 안전 하한을 다시 잴 때 쓴다.** 사이트가 리미터를 바꾸면 403 비율이
먼저 움직이고, 그것이 이 파일에 그대로 남는다.

## 7. 종료 코드

| 코드 | 뜻 |
|---|---|
| 0 | 정상 |
| 1 | `csv/merged_filtered.csv` 가 없다 |
| 2 | 차단이 실측과 다르게 굴어 **덜 걷었다.** 걷은 평점은 캐시에 저장됐고, **출력 CSV 는 안 바꿨다** — 다시 돌리면 이어받아 온전한 판정으로 만든다 |
| 3 | 이미 돌고 있다 |

`4`(건너뜀)는 이 단계에 없다 — 잡플래닛 **수집기**의 신호이지 여기 것이 아니다.

**`2` 는 실패로 센다.** 덜 걷힌 평점으로 거른 결과를 온전한 것으로 읽으면 안 된다.
오케스트레이터는 이 단계를 **거르기가 성공했을 때만** 부른다.
