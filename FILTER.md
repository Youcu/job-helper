# 거르기

```bash
python3 filter.py
```

걷은 공고에서 **버릴 것을 버린다.** 두 가지만 한다 — 같은 공고 묶기, 정해진 낱말 빼기.

```
csv/merged_read.csv  ──중복 제거──▶  ──낱말 제외──▶  csv/merged_filtered.csv
      710행                674행           561행     +  csv/filter_report.csv (149행)
```

숫자는 2026-09-10 실측이다. 실행할 때마다 화면에 찍히므로 지금 값은 돌려 보면 안다.

## 무엇을 하고, 무엇을 안 하나

| | |
|---|---|
| **한다** | 같은 공고를 한 행으로 묶는다 · 정해진 낱말이 든 공고를 뺀다 · **뺀 것을 전량 보고 CSV 에 남긴다** |
| **안 한다** | 점수 매기기 · 회사 평판 조회 · 모델 판단 · 원문 고치기 |

**제자리에서 고치지 않는다.** 이 단계는 행을 없앤다. 입력을 덮어쓰면 없어진 행의 원본이
사라져 왜 없어졌는지 되짚을 수 없다.

---

## 1. 같은 공고 묶기

키는 **정규화한 기업명 + 공고명**이다 (`filter_words.key`).

```
기업명   법인 표기와 공백을 지우고 소문자로   `(주)안랩` `㈜안랩` `주식회사 안랩` → `안랩`
공고명   공백만 지우고 소문자로              대괄호는 그대로 둔다
```

**왜 공고명 하나로 안 묶나.** 실측 710행에서 같은 공고명을 쓴 그룹이 37개였는데, 그중
**35개는 같은 공고가 여러 사이트에 올라간 것**이고 기업명 표기만 달랐다. 나머지 중 4개는
**진짜 다른 회사가 같은 제목을 쓴 것**이다.

| 공고명 | 회사 |
|---|---|
| `SW Engineer` | 비햅틱스 / 퀄리타스반도체 |
| `각 부문별 채용` | 웨어밸리 / 형원이엔지 |
| `백엔드 개발자 (신입)` | 이너버스 / 티엔에이치 |
| `입찰 조달 데이터 기반 웹서비스 개발 및 운영자 모집` | 케이비드 / 한국전자입찰연구소 |

공고명만 쓰면 이 넷에서 멀쩡한 공고가 지워진다. 지우는 판단에 "모르겠으면 지운다" 는 없다.

**대괄호는 안 뗀다.** `[인턴] Backend Engineer` 와 `Backend Engineer` 는 다른 자리다.

**같은 사이트 안의 중복은 안 묶는다.** 한 사이트가 같은 회사·같은 제목으로 두 번 올렸다면
대개 부문이 다른 별개 공고다 (실측 2건). 사이트가 다를 때만 같은 공고로 본다.

**남기는 행**은 `지원자격 + 우대사항 + 기술스택` 길이가 가장 긴 것이다 — 본문이 가장 온전한
사본이 남는다. 묶을 때 **빈 칸만 채우고 값이 있는 칸은 안 덮는다.** 날짜 두 칸만 예외로,
`최초수집일` 은 가장 이른 것, `최종확인일` 은 가장 늦은 것을 쓴다.

**기업명과 공고명이 둘 다 비면 안 묶는다.** 계약상 두 칸은 빌 수 있고
(`docs/convention/02-data-contract.md`), 키가 없는 행끼리 묶으면 아무 상관 없는 공고가
한 행으로 뭉개진다.

---

## 2. 낱말 빼기

**보는 칸은 셋뿐이다** — `공고명` `지원자격` `우대사항`. 사용자가 지정한 칸이다.
`기업명`·`기술스택`·`근무지` 에는 안 건다. `에스아이알소프트`·`넥스에스아이` 같은 회사가
있고, 그중에는 본문에 SI 를 한 번도 안 쓰는 공고가 있다 — 이름만으로 자르면 근거 없이
자르는 것이다.

| 낱말 | 실측 적중 |
|---|---|
| SI | 40행 |
| 고객사 | 31행 |
| 전문연구요원 | 27행 |
| 병역특례 | 25행 |
| 보충역 | 15행 |
| 산업기능요원 | 15행 |
| SM | 12행 |
| 현역 | 9행 |
| 파견 | 7행 |

(행이 여러 낱말에 걸릴 수 있어 합이 뺀 행 수보다 크다.)

### `SI` 와 `SM` 은 경계를 둔다

두 글자짜리 영문 약자는 아무 영단어에나 들어간다. **앞뒤에 ASCII 영숫자가 없을 때만**
낱말로 본다.

```python
(?<![A-Za-z0-9])SI(?![A-Za-z0-9])
```

| 규칙 | 결과 |
|---|---|
| 대소문자 무시 (`re.I`) | `si` 가 **110행** — vision · design · business · size · extension · assignment · physics · ansible · simulation · version |
| 대소문자만 구분 | `SI` 에 SIEMENS · SIEM · SIMPAC · ASIC · SSIM · VLSI · WSI 가 남는다 |
| **ASCII 경계 + 대소문자 구분** | **43행.** 위의 것이 전부 빠진다 |

`SM` 도 같다 — 대소문자 무시면 35행(isms · smart · smoothing · prisma · langsmith · plasma),
경계 규칙이면 12행.

**`\bSI\b` 는 안 쓴다.** 파이썬의 `\b` 는 **한글도 낱말 문자로 보기 때문에**
`SI프로젝트` · `SI업체` · `SM사업부` · `ㆍSI &` 앞뒤에 경계가 없어 놓친다 (실측 SI 3행 · SM 1행).
위 규칙은 `\b` 가 잡는 것을 하나도 안 놓치면서 그 넷을 더 잡는다.

### 한글 낱말은 경계를 안 둔다

실측으로 이것들을 부분으로 품은 다른 말이 없었다.

```
현역   → 현역 · 현역전직 · 현역x
파견   → 파견 · 파견근무 · 단기파견
고객사 → 고객사 · 고객사와 · 고객사에서 · 신규고객사
```

전부 같은 뜻이라 경계를 두면 오히려 놓친다. 다만 `병역특례` 는 `병역\s*특례` 로 띄어쓰기를
받는다 — `병역 특례` 로 쓴 공고가 있다. **`병영` 은 `병역` 이 아니다.**

---

## 3. 이 규칙이 **잡지 못하는 것**

낱말만 보고는 가릴 수 없는 것들이다. 실측으로 확인한 오탐:

- `SI 개발자 성향이 아닌 애자일 방식…` — SI 를 **부정**하는 문장인데 걸린다. 3행.
- 패션 VMD 공고의 `SI 채용` — 여기서 SI 는 Store Identity 다. 1행.
- `글로벌 고객사와 영어로 소통` 처럼 자체 제품 회사의 평범한 `고객사` 언급.

그래서 **뺀 행을 전량 `csv/filter_report.csv` 에 남긴다.** 걸린 낱말과 그 자리의 앞뒤 24자를
함께 적으므로, 파일 하나로 무엇을 잃었는지 되짚을 수 있다.

```
기업명, 공고명, 사이트명, URL, 판정, 걸린낱말, 근거
```

판정은 두 갈래다.

```
제외 · 중복    근거 = 남긴 행의 URL
제외 · 낱말    근거 = SI «… 공공기관 또는 SI 프로젝트 수행 경험 …»
```

낱말표를 되돌리려면 `filter_words.py` 의 `BANNED` 에서 줄을 빼면 된다.

---

## 4. 차례가 왜 이런가

**중복 제거가 먼저다.** 반대로 하면 같은 공고의 사본마다 따로 판정되고, 사이트별로 걷어 온
본문 길이가 달라 **한쪽만 걸리는 일**이 생긴다. 먼저 묶어 가장 온전한 본문 하나로 판정한다.

## 5. 종료 코드

| 코드 | 뜻 |
|---|---|
| 0 | 정상 |
| 1 | `csv/merged_read.csv` 가 없다 — 먼저 수집을 돌려야 한다 |
| 3 | 이미 돌고 있다 (`_common/runlock.py` 의 `ALREADY_RUNNING`) |

`2`(부분 실패)와 `4`(건너뜀)는 **이 단계에 없다.** 표에 적어 두면 있지도 않은 일이 화면에 찍힌다.

오케스트레이터는 이 단계를 자식 프로세스로 부르고, **앞 단계가 실패하면 부르지 않는다** —
앞이 죽었으면 `merged_read.csv` 에 있는 것은 지난 실행이 남긴 것이다.
