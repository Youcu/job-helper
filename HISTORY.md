# 이력 쌓기와 뒷정리

```bash
python3 history.py
```

파이프라인이 **최종본까지 만든 뒤** 돈다. 이력을 쌓고 사이트별 수집본을 치운다.

```
csv/merged_read.csv       ──누적──▶  history/history_read.csv        전처리 이전
csv/merged_career.csv     ──누적──▶  history/history_final.csv       최종본
리포트 셋                 ──누적──▶  history/history_dropped.csv     왜 빠졌나
csv/same_names.csv        ──누적──▶  history/history_same_names.csv  동명 회사 후보
job_sites/*/csv/*_post.csv  ──삭제──
```

## 무엇을 하고, 무엇을 안 하나

| | |
|---|---|
| **한다** | 네 파일에 누적 · 사이트별 수집본 삭제 |
| **안 한다** | **`./csv` 를 건드리지 않는다.** 중간 산출물도 리포트도 그대로 둔다 |

`./csv` 에는 **이번 실행의 산출물 아홉 개**가 지금 모양 그대로 남는다. 사람이 보고 싶은
것은 지금 돌린 파이프라인의 결과이고, 그것을 분석용으로 쓸지 점검용으로 쓸지는 쓰는
사람이 정한다 (2026-09-12 사용자).

**지우는 것은 `*_post.csv` 뿐이다.** 같은 폴더의 다른 파일은 안 건드린다 — 이 단계가 만든
것이 아니다.

---

## 1. 누적의 자리를 여기로 옮겼다

전에는 **사이트별 CSV 다섯이 각자 누적 저장소**였다. `_common/store.save()` 가 기존 파일과
병합해 `최초수집일` 을 지키고 30일 보존을 했다.

그 다섯을 매 실행 지우면서 누적의 자리를 `./history` 한 곳으로 옮겼다 — 쌓이는 곳이
다섯이 아니라 하나가 되어 **오히려 단순해진다.**

## 2. 사이트 CSV 를 지우면 잃는 것 — 그리고 어떻게 지켰나

그냥 지우면 두 가지를 잃는다.

```
최초수집일 이 항상 오늘이 된다      → 칸의 뜻이 없어진다
내려간 공고가 즉시 사라진다         → 전에는 30일 남았다
```

**30일 보존**은 `store.merge()` 가 `history_read.csv` 안에서 그대로 해 준다.

**`최초수집일`** 은 오케스트레이터가 합칠 때 되살린다
(`job_crawling_ochestrator._seed_first_seen`) — `history_read.csv` 를 URL 로 찾아
그 공고를 처음 본 날을 `merged.csv` 에 넣는다. 이력에 없는 공고는 오늘 처음 본 것이
맞으므로 손대지 않는다.

실측(2026-09-12): 768행을 합치면서 **730행의 날짜를 되살렸고**, 남은 38행만 오늘이 됐다.

### 여기 결함이 하나 있었다

`store.merge()` 는 **처음 보는 행의 `최초수집일` 을 오늘로 덮는다.** 스크래퍼가 날짜 없이
행을 주기 때문이고, 그쪽에서는 맞다.

그런데 이력에 들어오는 행은 `./csv` 에서 오므로 **이미 진짜 날짜를 갖고 있다.** 그대로
두면 이력을 처음 만드는 날 **전부 오늘로 뭉개진다** — 실제로 그랬다. 그래서 병합 뒤
`_keep_earliest()` 로 둘 중 이른 쪽을 남긴다.

`tests/test_history.py` 의 `test_BOUNDARY_first_seen_survives_the_first_accumulation` 이
이것을 굳힌다.

## 3. 리포트 셋을 한 파일로 합친다

세 리포트는 칸이 서로 다르다 — `filter_report` 는 `걸린낱말`, `rating_report` 는 `평점`·
`회사id`, `core_stack_report` 는 `기술스택`. 그런데 **공통 다섯 칸은 같다.**

```
history/history_dropped.csv
    기록일 · 단계 · 기업명 · 공고명 · 사이트명 · URL · 판정 · 근거
```

나머지 칸은 `근거` 한 칸에 글로 넣는다 (`평점=2.0 · 회사id=7 · 찾은방법=원문`).

세 파일로 나눠 쌓으면 "이 공고가 왜 빠졌나" 를 찾을 때 **세 군데를 뒤져야 한다.**
한 파일이면 URL 하나로 답이 나온다. 칸 구조가 온전한 원본은 `./csv` 에 그대로 있다.

`(URL, 단계)` 로 묶고 **최신 판정을 남긴다** — 같은 공고가 같은 단계에서 다시 빠지면
최신이 맞고, 단계가 다르면 둘 다 남는다.

## 4. 최종본까지 나왔을 때만 돈다

오케스트레이터는 **핵심 기술 단계가 성공했을 때만** 이 단계를 부른다.

중간에 멈춘 실행의 반쪽 결과를 이력에 섞으면 나중에 "그때 이 공고가 없었다" 를 거짓으로
읽는다. 그리고 이 단계는 **사이트 CSV 를 지우므로** 더 위험하다 — 반쪽을 쌓고 원본까지
지우면 다시 걷는 수밖에 없다.

혼자 돌릴 때도 `merged_read.csv` 와 `merged_career.csv` 가 **둘 다** 있어야 돈다.

## 5. 종료 코드

| 코드 | 뜻 |
|---|---|
| 0 | 정상 |
| 1 | `merged_read.csv` 나 `merged_career.csv` 가 없다 — **파이프라인이 안 끝났다** |
| 3 | 이미 돌고 있다 |

`1` 로 멈출 때는 **사이트 CSV 를 지우지 않고 이력도 안 만든다.**

## 6. 테스트

```bash
python3 tests/run_all.py
```

`tests/test_history.py`. 진짜 파일을 안 지운다 — 경로를 인자로 넣고, `sweep=False` 로
치우기를 끌 수 있다.

이 단계는 **파일을 지우므로**, 테스트가 "잘 지우는가" 보다 **"지우기 전에 이력이 제대로
쌓였는가"** 와 **"지우면 안 되는 것을 안 지키는가"** 를 더 많이 굳힌다.
