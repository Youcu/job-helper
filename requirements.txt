# 이 저장소가 실제로 import 하는 것 넷뿐이다.
#
# **`.venv` 를 보고 베끼지 마라.** 거기에는 pandas·numpy·rapidfuzz·kiwipiepy 가 섞여 있는데
# 추적되는 코드 중 어느 것도 그것들을 import 하지 않는다 (rapidfuzz 는 `_common/normalize.py`
# 주석에서 "안 쓴다" 고 언급될 뿐이다). 옛 실험이 남긴 것이다.
#
# 버전을 박는 이유는 **재현성**이다. 같은 커밋을 clone 한 두 사람이 다른 결과를 보면
# 그것이 코드 탓인지 라이브러리 탓인지 가릴 수 없다. 올릴 때는 테스트를 돌려 보고 올린다.

requests==2.34.2          # 사이트 HTTP. wanted 만 쓰고 나머지는 urllib
tqdm==4.70.0              # 진행 막대
python-dotenv==1.2.3      # .env 를 읽는다 (환경변수는 안 건드린다 — D-08)
Pillow==12.3.0            # 그림 본문 자르기. **테스트에도 필요하다** (아래)

# Pillow 는 "이미지 판독 단계에만 필요한 선택 항목" 이 아니다.
# `tests/run.py` 의 MODULES 에 test_image_* 가 들어 있어서, 없으면 루트 테스트가
# import 단계에서 통째로 죽는다. 이미지 단계를 안 쓸 사람도 깔아야 한다.
