# 경력 거르기

```bash
python3 career.py
```

**신입이라 적어 놓고 경력을 요구하는 공고**를 뺀다. 파이프라인의 마지막 거르기다.

```
csv/merged_core.csv  ──▶  csv/merged_career.csv    남은 공고
                     +    csv/career_report.csv    뺀 이유 전량
```

## 무엇을 하고, 무엇을 안 하나

| | |
|---|---|
| **한다** | 후보를 코드로 좁히고 · 판정은 모델이 하고 · **뺀 것을 전량 보고 CSV 에** |
| **안 한다** | 낱말만으로 자르지 않는다 · **애매하면 안 뺀다** |

---

## 1. 왜 코드만으로 안 되나

실측(2026-09-13, `merged_core.csv` 66행 — 전부 "신입 지원 가능"이라 적힌 공고다).

| 기준 | 걸리는 수 |
|---|---|
| `경력`·`경험`·`능숙` 을 다 잡으면 | **42행 (64%)** |
| **재직 연수**를 요구하는 말만 | **10행 (15%)** |

**"경험" 과 "경력 연수" 는 다른 것이다** (2026-09-13 사용자).

```
"개발 경험이 있으신 분" · "Spring 에 능숙한 분"    → 갖춰야 할 역량. 신입도 가진다
"경력 3년 이상" · "5~10년 정도"                   → 재직 연수. 이것만이 모순
```

그런데 연수로 좁혀도 뚫린다.

```
• 경력직 지원자의 경우, 필요 시 레퍼런스 체크 진행 예정
```

`경력직` 이 들어 있지만 **경력직도 받는다**는 말이지 요구가 아니다. 규칙을 아무리 더해도
다음 형태에서 또 뚫린다. 그래서 **좁히는 일은 코드가, 판정은 모델이** 한다.

## 2. 코드가 좁히는 규칙 셋

`career_words.py` 에 있다. 셋 다 실측에서 나왔다.

| 규칙 | 왜 | 효과 |
|---|---|---|
| **지원자격만 본다** | 우대는 없어도 지원할 수 있어 모순이 아니다 — `교보다솜케어` 는 자격이 `경력 무관` 인데 우대에만 `경력 1년 이상` 이 있었다 | |
| **연수를 요구하는 말만** | `4년제`·`27년 2월` 은 학력·날짜다 | 42 → 14행 |
| **같은 줄에 신입이 있으면 뺀다** | `신입 / 경력 1년 이상 ~ 3년 이하` 는 신입도 받는다는 뜻 | 14 → 10행 |

## 3. 모델에게 무엇을 시키나

`career.build_prompt()`. **판정 기준을 프롬프트에 못 박는다** — 안 적으면 모델이
`개발 경험이 있으신 분` 을 경력 요구로 읽는다.

```
- `경력 3년 이상` 처럼 재직 연수를 요구하면 모순이다
- `개발 경험이 있으신 분` 은 역량이지 경력이 아니다. 모순이 아니다
- **경력을 대신할 길이 있으면 모순이 아니다** — `신입 또는 경력 3년` ·
  `경력 3년 또는 이에 준하는 역량` · `석사 이상 또는 경력 3년`.
  `또는`·`혹은` 뒤를 반드시 읽어라
- `경력직 지원자의 경우 …` 처럼 경력직을 받는다는 말은 요구가 아니다
- **애매하면 false 를 내라**
```

### 실측 판정 (2026-09-13, 후보 10건)

| | 건 |
|---|---|
| 제외 | 8 |
| 남김 | 2 — `경력직 지원자의 경우 …` · `경력 2년 이상 **또는 이에 준하는 개발 역량**` |

**코드로 못 잡던 둘을 모델이 정확히 살렸다.** 사람이 10건을 눈으로 다시 본 결과와 전부 같았다.

## 4. 애매하면 **남긴다**

지우는 판단에 "모르겠으면 지운다" 는 없다 (2026-09-13 사용자).

놓친 공고는 사람이 목록에서 보고 넘기면 그만이지만, **지워진 공고는 있었다는 사실조차
안 남는다.** 그래서 세 자리에서 남기는 쪽으로 기운다.

```
모델이 애매하다고 하면        →  남긴다 (프롬프트가 그렇게 시킨다)
모델에게 못 물어봤으면        →  남긴다. **못 물어본 것을 모순으로 읽으면 안 된다**
후보로도 안 걸렸으면          →  남긴다
```

## 5. 캐시 — **규칙판을 함께 적는다**

`cache/career_judge.json` 에 `URL → 판정` 을 쌓는다. **같은 공고를 두 번 물으면 답이
달라질 수 있어서** 재현성이 걸려 있다.

```json
{"모순": true, "근거": "…", "걸린줄": "…", "규칙판": 2, "판정일": "2026-09-13"}
```

**`규칙판` 이 없으면 프롬프트를 고쳐도 안 먹는다.** 실제로 그랬다 — `또는` 뒤의 대체
경로를 읽으라는 지시를 넣었는데 캐시에 든 12건이 옛 판정을 계속 냈다. **고침이 조용히
사라지는 종류**라 다음에 프롬프트를 손봐도 "왜 안 바뀌지?" 만 남는다.

**규칙(프롬프트)을 손대면 `career.RULES_VERSION` 을 올려라.** 그러면 다음 실행이 다시 묻는다.

**한 건 끝날 때마다 저장한다** — 중간에 죽어도 다시 돌리면 이어받는다.

### 실측 두 판 (2026-09-13)

| 판 | 제외 | 무엇이 달랐나 |
|---|---|---|
| 1 | 10 | `또는` 뒤를 안 읽어 `석사 이상 또는 3년 경력` 을 모순으로 봤다 |
| **2** | **9** | 대체 경로(학위·역량)를 읽는다 |

12건을 두 판으로 판정해 **1건만 바뀌었다.** 나머지 11건은 흔들리지 않았다.

## 6. `claude` 가 없으면 멈춘다

거르지 않고 통과시키면 **사람은 걸렀다고 믿는데 실제로는 안 걸린 파일**을 받는다.
`core_stack` 이 `.env` 가 비었을 때 멈추는 것과 같은 판단이다.

## 7. 종료 코드

| 코드 | 뜻 |
|---|---|
| 0 | 정상 |
| 1 | `csv/merged_core.csv` 가 없거나 `claude` 명령을 못 찾음 |
| 3 | 이미 돌고 있다 |

`2`(부분 실패)는 없다. 한 건을 못 물어보면 **그 행을 남기고 계속 간다** — 판정 실패
건수는 화면에 찍힌다.

## 8. 테스트

```bash
python3 tests/run_all.py
```

`tests/test_career_words.py` (말 규칙) · `tests/test_career.py` (절차). 모델도 그물도
안 탄다 — 판정 함수를 인자로 넣는다.

**"모델이 말 안 한 것을 지우지 않는가"** 를 가장 많이 굳힌다 — 못 물어봤을 때, 답이
깨졌을 때, `claude` 가 아예 없을 때.
